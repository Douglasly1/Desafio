

<p style="color: green"><%= notice %></p>

<div class="card">
  <div class="card-header">
    <h3>Setores</h3>
  </div>
  <div class="card-body">
    <h5 class="card-title"><%= render @setor %></h5>


<div class="d-flex justify-content-between">
<div class="d-flex gap-2">
      <%= link_to "Editar", edit_setor_path(@setor), class: "btn btn-primary" %>
      <%= button_to "Apagar" , @setor, method: :delete, class:"btn btn-danger" %>
</div>

<div class="d-grid gap-2 d-md-flex justify-content-md-end">
      <%= link_to "Voltar", setors_path, class:"btn btn-outline-primary" %>
</div>
</div>     
</div>
